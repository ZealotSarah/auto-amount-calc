# 基金金额自动测算工具 - 设计文档（v0.7）

## 1. 目标与边界

为每个待计算的 Excel 文件新增一个独立的结果 Sheet（建议命名 `基金测算`）。工具读取原始明细，识别通用、串换或固定比例规则以及住院或门诊数据，匹配相应计算口径，计算基金金额，并在同一 Sheet 中提供透视汇总。

本期只处理输入工作簿，不修改原有业务 Sheet 的数据、公式或格式；结果始终写入新增 Sheet。每次成功计算都会先校验新工作簿，再将测算前原文件原样保存为同目录、带时间戳的备份，最后替换工作文件。若替换失败则恢复原文件。若已存在同名结果 Sheet，可覆盖结果 Sheet，或生成带时间戳的新 Sheet。

## 2. 已核对的数据来源

### 2.1 原始 Excel 的稳定核心字段

样例中表头存在多个版本、部分字段带有 `1`、`2` 后缀，但以下字段可作为标准映射目标：

| 标准字段 | 常见原始表头 | 用途 |
| --- | --- | --- |
| 就诊标识 | `就诊ID` | 明细归属及透视计数 |
| 医疗类别 | `医疗类别`、`医疗类别名称` | 优先识别代码，兼容名称，判断住院/门诊 |
| 医院等级 | `医院等级` | 匹配住院比例 |
| 险种类型 | `险种类型`、`险种类别`、`INSUTYPE`、`insutype` | 精确识别职工、城乡居民名称或 310、390 代码 |
| 参保地 | `参保地`、`参保地代码` | 统筹区匹配的候选字段 |
| 医疗费总额 | `医疗费总额` | 门诊基金支付率分母 |
| 基金支付总额 | `基金支付总额` | 门诊基金支付率分子 |
| 符合范围金额 | `符合范围金额`、`符合范围金额1` | 通用规则的计算基数 |
| 单价 | `单价`、`单价1` | 串换规则无违规金额时的原单价 |
| 数量 | `数量`、`数量1` | 通用、固定比例规则汇总原数量；串换规则汇总扣减后的数量 |
| 违规金额 | `违规金额`（若存在） | 串换规则优先计算基数 |

样例还显示：有的“串换/超量”规则只增加一个规则计算列，例如 `床位数量-住院天数`，而不是直接提供 `违规金额`。因此工具不能把规则计算列误认为违规金额；只有精确匹配或由用户映射为“违规金额”的列才可直接使用。

### 2.2 住院比例来源

比例来源为 `示例/24年25年目录内（费用）住院基金支付比例.pdf`。该表以“统筹区 × 医院等级（三级/二级/一级）× 险种（职工/居民/合计）”给出比例，覆盖河北省、14 个统筹区及省本级。

示例：张家口市三级机构的职工/居民/合计比例分别为 86.49%、70.32%、75.63%；二级机构为 87.67%、79.08%、80.97%；一级机构为 92.66%、90.30%、90.70%。

为避免运行时从 PDF 解析造成不稳定，首次实施应将该页人工复核后固化为工具内置的比例表（CSV/JSON），同时在结果 Sheet 记录“比例版本：24-25 年”。后续换年度只更新比例配置，不改计算代码。

## 3. 使用流程

1. 用户选择一个或多个 Excel 文件。
2. 用户选择源数据 Sheet。选择“自动选择”时，仅在全工作簿恰好有一个字段完整且唯一的业务 Sheet 时继续；检测到多个可用业务 Sheet 时必须明确选择，不能按行数或顺序静默猜测。
3. 工具扫描所选 Sheet 前 10 行定位表头并自动匹配常用字段。同一目标字段出现多个精确或前缀候选列时停止该文件，列出候选列位置，不静默取最左列。
4. 用户在参数页手动选择：规则大类（通用/串换/固定比例）、业务类型（自动识别/住院/门诊）、用于选择固定报销比例的参保地、医疗机构级别（一级/二级/三级）。参保地和医疗机构级别不提供默认值，必须显式选择。串换时必须输入不小于 0 的有限扣减数量；无违规金额列时还必须输入不小于 0 的有限扣减单价。
5. 工具校验数据、匹配比例、按结果维度汇总计算，并在每个文件中新建 `基金测算` Sheet。
6. 工具先将结果保存到同目录临时文件，重新打开并校验结果 Sheet 和基金金额合计；校验通过后保留测算前备份并替换工作文件。
7. 用户在结果 Sheet 查看八列数据透视汇总和运行异常统计；未能计算的行不得计入基金金额合计。

批量处理时，每个文件独立输出和记录异常，单个文件失败不得中断其他文件。

## 4. 计算规则

### 4.1 计算前通用约定

- 按“医疗机构编码、险种类别、医疗类别”分组，先汇总组内计算基数，再乘该组报销比例；每个汇总组的最终“基金金额”按财务口径四舍五入至 2 位，不对明细行金额逐行取整。
- 通用和固定比例规则的“数量总和”为分组原数量合计；串换规则先对每条明细计算“原数量 − 扣减数量”，再汇总扣减后的数量。扣减后的数量为负数时，该明细不计入汇总并记录异常。
- 负数基数或负数结果不直接计入；标记为“需复核”。
- 自动识别时优先读取 `医疗类别` 代码：`11`（普通门诊）、`14`（门诊慢特病）、`51`（生育门诊）归为门诊；`21`（普通住院）、`22`（外伤住院）、`52`（生育住院）归为住院。兼容文本、整数及整数型浮点代码。
- 代码未匹配时，依次检查 `医疗类别` 的文字内容和 `医疗类别名称`，包含“住院”或“门诊”则归入对应类别。只有名称列也可识别；代码与名称冲突时以已支持代码为准。均无法识别则记录异常，不推测未知代码含义。手动选择住院或门诊时仍以用户选择为准。
- 比例表按“参数页参保地、参数页医疗机构级别、险种类型”匹配。通用、串换规则仅住院使用该比例；固定比例规则的住院和门诊均使用该比例。参数页参保地不用于筛选、分组或覆盖原始 Excel 数据，也不从原始 Excel 自动选择。原始表中的 `参保地`、`医院等级`仅作为核验信息。
- 门诊和住院均使用精确匹配，不按“包含职工/居民”等子串模糊判断。职工允许值：`310`、`职工`、`城镇职工`、`职工基本医疗保险`、`城镇职工基本医疗保险`；城乡居民允许值：`390`、`城乡`、`居民`、`城乡居民`、`城乡居民基本医疗保险`。对应 SQL：`INSUTYPE = '310'` 输出“职工”，`INSUTYPE = '390'` 输出“城乡”，其他值输出“其他”。
- 代码支持文本、整数和整数型浮点数（例如数值 390.0），名称仅去除首尾空白后精确匹配。其他、空值及未列出的名称或代码在金额校验和汇总前排除，不计异常，不使用“合计”比例，也不计入医疗总额、基金金额及报销比例。

### 4.2 规则矩阵

| 大类 | 住院基金金额 | 门诊基金金额 |
| --- | --- | --- |
| 通用 | 分组符合范围金额合计 × 住院基金支付比例 | 分组符合范围金额合计 ×（分组基金支付总额合计 ÷ 分组医疗费总额合计） |
| 串换，存在违规金额列 | 分组违规金额合计 × 住院基金支付比例 | 分组符合范围金额合计 ×（分组基金支付总额合计 ÷ 分组医疗费总额合计） |
| 串换，不存在违规金额列 | 分组扣减计算基数合计 × 住院基金支付比例 | 分组符合范围金额合计 ×（分组基金支付总额合计 ÷ 分组医疗费总额合计） |
| 固定比例 | 分组符合范围金额合计 × 固定比例 | 分组符合范围金额合计 × 固定比例 |

串换住院同样先汇总分组计算基数，再乘住院基金支付比例：存在违规金额列时汇总违规金额；无违规金额列时汇总各明细按扣减参数得到的计算基数。原单价和原数量分别取映射后的单价、数量列；扣减单价、扣减数量为本次运行的用户输入参数，应写入结果 Sheet 顶部，确保可追溯。通用和串换规则的门诊先汇总符合范围金额，再乘分组动态报销比例。固定比例规则不使用门诊动态比例，住院和门诊都按“参保地 × 医疗机构级别 × 险种”从当前比例表取值，并以分组符合范围金额合计作为计算基数。

## 5. 结果 Sheet 设计

单个新增 Sheet 内分为三块，避免散落在多个 Sheet：

### A. 参数与审计区（顶部）

- 原文件名、源 Sheet、生成时间、工具版本；
- 规则大类、业务类型选择、用于报销比例匹配的手动参保地、医疗机构级别、扣减单价、扣减数量；
- 比例表版本；
- 总记录数、成功计算数、异常数、基金金额合计、排除险种记录数。处理记录数 = 成功计算数 + 异常数 + 排除险种记录数；排除数同时显示在运行日志中。

### B. 数据透视区（唯一业务结果表）

该区仅保留以下八列，不输出额外的明细列：

| 列名 | 汇总或取值规则 |
| --- | --- |
| 医疗机构编码 | 按原始 `定点编码` 分组 |
| 医疗机构名称 | 同一编码出现多个名称时，固定取该编码在源数据中的首个非空名称 |
| 险种类别 | 仅对职工和居民记录按原始 `险种类型` 分组；其他险种不进入汇总 |
| 医疗类别 | 住院或门诊；作为分组字段，确保两类记录的报销比例分别显示 |
| 医疗总额 | `医疗费总额` 求和 |
| 数量总和 | 通用、固定比例规则汇总原数量；串换规则汇总各明细的 `原数量 − 扣减数量` |
| 基金金额 | 先汇总该分组的计算基数，再乘该组报销比例，最后四舍五入至 2 位 |
| 报销比例 | 固定比例规则的住院和门诊均显示比例表匹配值；其他规则住院显示比例表匹配值，门诊显示 `基金支付总额 ÷ 医疗费总额` 的加权比例（即分组基金支付总额之和 ÷ 分组医疗费总额之和） |

数据透视按“医疗机构编码、险种类别、医疗类别”分组。医疗机构名称不作为独立分组键，以保证同一机构编码即使出现多个名称也只输出一个固定名称。医疗总额、数量和基金金额分别按上述规则汇总；住院和门诊分别占一行，报销比例按上表定义显示，不能对明细百分比做简单平均。

对于结果量很大的工作簿，优先生成 Excel 原生数据透视表；如运行环境无法创建原生透视表，则生成具有相同八列、相同分组和汇总规则的普通汇总表，并在参数与审计区提示“非原生透视表”。

## 6. 校验与异常处理

| 校验项 | 处理方式 |
| --- | --- |
| 找不到必填列 | 停止该文件，展示缺失列和可选映射列 |
| 同一目标字段匹配到多个候选列 | 停止该文件，展示字段名、候选表头及列号，要求整理表头后重试 |
| 其他、空值或无法识别的险种 | 门诊、住院均排除，仅统计排除数，不计异常 |
| 自动识别医疗类别失败 | 支持代码和名称均未匹配时记录异常，提示原始 Excel 行号 |
| 未选择用于报销比例匹配的参保地或医疗机构级别 | 停止该文件，提示在参数页完成手动选择 |
| 住院比例未匹配 | 该行基金金额留空，记录未匹配的统筹区/医疗机构级别/险种 |
| 医疗费总额为 0 或为空 | 门诊行不计算，记录异常 |
| 基金支付总额为空 | 门诊行不计算，记录异常 |
| 缺少数量列 | 停止该文件，提示缺少必填列“数量” |
| 串换未输入扣减数量 | 停止该文件，要求输入扣减数量 |
| 串换无违规金额且未输入扣减单价 | 停止该文件，要求输入扣减单价 |
| 扣减单价或扣减数量为负数、NaN 或无穷大 | 参数校验阶段停止运行，不打开和修改工作簿 |
| 扣减后单价或数量为负 | 该明细不计算，记录异常 |
| 同一工作簿有多个可用业务 Sheet | 自动选择时停止该文件，要求用户明确选择源 Sheet；既有 `基金测算` Sheet 不参与候选 |
| 保存后结果 Sheet 缺失或合计不一致 | 停止替换，保留原文件不变 |
| 原文件被 Excel 占用或文件切换失败 | 停止该文件；若已开始切换则从测算前备份恢复原文件 |

## 7. 已确认及待处理的业务口径

已确认：

1. 通用和串换规则的门诊基金金额为：`分组符合范围金额合计 ×（分组基金支付总额合计 ÷ 分组医疗费总额合计）`。
2. 固定比例规则的门诊与住院计算方式一致：`分组符合范围金额合计 × 比例表匹配值`，不再动态计算门诊比例。
3. 数据透视结果仅输出第 5 节规定的八列。
4. 医疗机构级别由参数页选择，用于住院比例匹配。

参保地由用户在参数页手动选择，选项使用 PDF 中的统筹区文字（如“张家口市”），用于选择比例表中的匹配值；无需从原始 Excel 的参保地代码或文字自动匹配。

## 8. 建议实现方案与验收标准

建议制作本地 Windows 小工具：Python 负责字段读取、计算与审计，Excel 自动化负责新增 Sheet 和原生透视表；无需改造现有原始数据文件。首版界面仅保留文件选择、规则选择、扣减参数、字段确认和运行日志，避免过度设计。

首版验收：

1. 能对样例住院通用文件识别 `符合范围金额1`、医院等级和险种，并给出与 PDF 匹配的比例。
2. 能对含 `违规金额` 的串换文件优先使用该列；无该列时使用扣减参数公式。
3. 能在每个输入文件中新建一个结果 Sheet，原始 Sheet 无变化；成功运行后生成与测算前文件字节一致的时间戳备份。
4. 汇总金额等于各分组“计算基数合计 × 分组报销比例”四舍五入至 2 位后的合计，且异常记录可定位到原始行。
5. 门诊按 `分组符合范围金额合计 ×（分组基金支付总额合计 ÷ 分组医疗费总额合计）` 计算，并用至少一份门诊样例人工复算比对通过。
6. 自动识别优先使用代码，兼容名称和仅名称列；代码与名称冲突时已支持代码优先。其他险种在门诊和住院中均排除。
7. 两份现有示例使用通用、自动识别、廊坊市、三级参数，在临时副本上验证：02 文件处理 17,226 条，成功 17,218 条，排除 8 条，异常 0 条，基金金额 418,998.73；03 文件处理 17,132 条，成功 17,126 条，排除 6 条，异常 0 条，基金金额 127,218.35。排除数包含门诊和住院其他险种。两份结果均按分组汇总后计算，并通过汇总金额核对和原始业务数据一致性检查。
8. 通用和固定比例规则的数量总和等于分组原数量合计；串换规则的数量总和等于分组内各明细扣减后数量的合计。
9. 多个可用业务 Sheet、同一字段多个候选列、未选参保地或机构级别、负数及非有限扣减参数均会在修改文件前被拒绝。
10. 固定比例规则下，同一参保地、医疗机构级别和险种的住院与门诊使用相同固定比例，门诊医疗费总额为 0 时也不触发动态比例除零校验。

## 9. 打包

安装 `requirements.txt` 后运行 `./build_exe.ps1`，默认使用 PATH 中的 Python，也可通过 `-PythonPath` 指定解释器。脚本从项目目录运行 PyInstaller，生成 `dist/基金金额自动测算工具.exe`；打包失败时返回错误，不显示成功提示。
